{% extends 'base.html' %}
{% block body %}
  <script>
    function mouseDown(e) {
      e = e || window.event;
      if (e.which == 3) {
        alert("right click!");
      }
      return false;
    }
  </script>
  Robots:
  <table border=1><tr><th>player name</th><th>program</th></tr>
  {% for p in robot_list %}
    <tr title="right click for edit or delete" onmousedown="return mouseDown(event);"><td>{{ p.player_name }}</td><td>{{ p.program }}</td></tr>
  {% endfor %}
  </table>
{% endblock body %}
